// Paths
const root = __dirname
const src = root + '/src'
const site = `${src}/site`

// Constants
const host = 'localhost'
const port = '8080'
const worldSize = 10


module.exports = {
  root, src, site, host, port, worldSize
}